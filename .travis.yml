language: node_js
node_js:
  - "7"
  - "6"
  - "5"
  - "4"
cache:
  directories:
  - node_modules
  - "$HOME/.rvm"
before_install:
  - rvm install 2.2.5
  - rvm use 2.2.5
install:
  - yarn global coveralls
  - gem install fastlane
  - yarn
script:
  - yarn test
  - yarn coverage
sudo: false
